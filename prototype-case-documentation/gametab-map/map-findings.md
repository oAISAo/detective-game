1. Evidence polaroids
   the image has different heights based on the Evidence name. If the name is in two lines, the image is a square (1:1) which is correct. But if the test is just in one line, the image get's a bigger height and it's not the same as the width anymore. The name space should always have the height of two lines, and if the text is just one line it should be aligned to the bottom or the middle (vertically). The polaroids generally have different sizes based on if there's just one piece of evidence in a row or if there's two, and if there's a scrollbar on the left. The polaroid sizes and image sizes should be consistent, but if the screen has a bigger resolution they should dynamically increase.

2. Action button checkmark should be bigger

3. Notifications order
   Discovering `ev_mark_call_log` fires `trig_unlock_office` → Victim's Office becomes available
   Notification: "New location unlocked: Victim's Office" comes before the Notification about new evidence discovered.
   EXPECTATION: the new location unlocked notification should come after the evidence discovered notification

4. No actions remaining text should be italic

5. Map Happy Flow Step 6
   I sent the Shoe Print to the lab and then came to check the Hallway Flor and the Status is "Awaiting lab results"
   EXPECTATION: Hallway Floor Status shows as "Fully processed"

6. Evidence evolve further consideration 
   Should the polaroid clue card for an upgraded piece of evidence show a small "Lab analyzed ✓" indicator? This would make clear to the player why the card changed its name overnight. Optional but adds narrative polish.

7. Evidence Cards should be clickable and the click lead you to the evidence detail screen

8. Desk Drawer
   I don't like how desk Drawer just appears after the safe has been found. it's not clear for the player why this happens. Should we show the Desk Drawer target already in the beginning and have it locked (with a lock Icon)? and when the hidden safe is found in the Bookshelf, a notification can say a Key has been found. And then the lock icon can turn into a key icon and the user is able to click on it and inspect it?